# Quant Standard Library - Crypto Module
//
// Pure-Quant SHA-256 (FIPS 180-4).  Operates on the raw UTF-8 bytes of the
// input string and returns the lowercase hex digest.  32-bit arithmetic is
// emulated by masking with 0xFFFFFFFF.

// Rotate a 32-bit word right by n bits.
fn rotr(x: int, n: int) -> int {
    return ((x >> n) | (x << (32 - n))) & 0xFFFFFFFF;
}

fn hex_digit(d: int) -> str {
    return string.char_at("0123456789abcdef", d);
}

// Format a 32-bit word as 8 lowercase hex digits.
fn to_hex8(w: int) -> str {
    out: str = "";
    i: int = 7;
    while (i >= 0) {
        out = string.concat(out, hex_digit((w >> (i * 4)) & 0xF));
        i = i - 1;
    };
    return out;
}

// Returns the SHA-256 digest of `msg` as a 64-character hex string.
fn sha256_hex(msg: str) -> str {
    h: [int] = [
        0x6a09e667, 0xbb67ae85, 0x3c6ef372, 0xa54ff53a,
        0x510e527f, 0x9b05688c, 0x1f83d9ab, 0x5be0cd19
    ];
    k: [int] = [
        0x428a2f98, 0x71374491, 0xb5c0fbcf, 0xe9b5dba5, 0x3956c25b, 0x59f111f1,
        0x923f82a4, 0xab1c5ed5, 0xd807aa98, 0x12835b01, 0x243185be, 0x550c7dc3,
        0x72be5d74, 0x80deb1fe, 0x9bdc06a7, 0xc19bf174, 0xe49b69c1, 0xefbe4786,
        0x0fc19dc6, 0x240ca1cc, 0x2de92c6f, 0x4a7484aa, 0x5cb0a9dc, 0x76f988da,
        0x983e5152, 0xa831c66d, 0xb00327c8, 0xbf597fc7, 0xc6e00bf3, 0xd5a79147,
        0x06ca6351, 0x14292967, 0x27b70a85, 0x2e1b2138, 0x4d2c6dfc, 0x53380d13,
        0x650a7354, 0x766a0abb, 0x81c2c92e, 0x92722c85, 0xa2bfe8a1, 0xa81a664b,
        0xc24b8b70, 0xc76c51a3, 0xd192e819, 0xd6990624, 0xf40e3585, 0x106aa070,
        0x19a4c116, 0x1e376c08, 0x2748774c, 0x34b0bcb5, 0x391c0cb3, 0x4ed8aa4a,
        0x5b9cca4f, 0x682e6ff3, 0x748f82ee, 0x78a5636f, 0x84c87814, 0x8cc70208,
        0x90befffa, 0xa4506ceb, 0xbef9a3f7, 0xc67178f2
    ];

    // Build the padded message as a byte array.
    mlen: int = string.byte_len(msg);
    bytes: [int] = [];
    i: int = 0;
    while (i < mlen) {
        push(bytes, string.byte_at(msg, i));
        i = i + 1;
    };
    push(bytes, 0x80);
    while (len(bytes) % 64 != 56) {
        push(bytes, 0);
    };
    bitlen: int = mlen * 8;
    j: int = 7;
    while (j >= 0) {
        push(bytes, (bitlen >> (j * 8)) & 0xFF);
        j = j - 1;
    };

    nblocks: int = len(bytes) / 64;
    b: int = 0;
    while (b < nblocks) {
        base: int = b * 64;
        w: [int] = [];
        t: int = 0;
        while (t < 16) {
            off: int = base + t * 4;
            word: int = (bytes[off] << 24) | (bytes[off + 1] << 16)
                | (bytes[off + 2] << 8) | bytes[off + 3];
            push(w, word & 0xFFFFFFFF);
            t = t + 1;
        };
        while (t < 64) {
            s0: int = rotr(w[t - 15], 7) ^ rotr(w[t - 15], 18) ^ (w[t - 15] >> 3);
            s1: int = rotr(w[t - 2], 17) ^ rotr(w[t - 2], 19) ^ (w[t - 2] >> 10);
            push(w, (w[t - 16] + s0 + w[t - 7] + s1) & 0xFFFFFFFF);
            t = t + 1;
        };

        a: int = h[0];
        bb: int = h[1];
        c: int = h[2];
        d: int = h[3];
        e: int = h[4];
        f: int = h[5];
        g: int = h[6];
        hh: int = h[7];
        t = 0;
        while (t < 64) {
            bigs1: int = rotr(e, 6) ^ rotr(e, 11) ^ rotr(e, 25);
            ch: int = (e & f) ^ ((~e & 0xFFFFFFFF) & g);
            temp1: int = (hh + bigs1 + ch + k[t] + w[t]) & 0xFFFFFFFF;
            bigs0: int = rotr(a, 2) ^ rotr(a, 13) ^ rotr(a, 22);
            maj: int = (a & bb) ^ (a & c) ^ (bb & c);
            temp2: int = (bigs0 + maj) & 0xFFFFFFFF;
            hh = g;
            g = f;
            f = e;
            e = (d + temp1) & 0xFFFFFFFF;
            d = c;
            c = bb;
            bb = a;
            a = (temp1 + temp2) & 0xFFFFFFFF;
            t = t + 1;
        };

        h[0] = (h[0] + a) & 0xFFFFFFFF;
        h[1] = (h[1] + bb) & 0xFFFFFFFF;
        h[2] = (h[2] + c) & 0xFFFFFFFF;
        h[3] = (h[3] + d) & 0xFFFFFFFF;
        h[4] = (h[4] + e) & 0xFFFFFFFF;
        h[5] = (h[5] + f) & 0xFFFFFFFF;
        h[6] = (h[6] + g) & 0xFFFFFFFF;
        h[7] = (h[7] + hh) & 0xFFFFFFFF;
        b = b + 1;
    };

    out: str = "";
    i = 0;
    while (i < 8) {
        out = string.concat(out, to_hex8(h[i]));
        i = i + 1;
    };
    return out;
}
